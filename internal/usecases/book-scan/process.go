package book_scan

import (
	"context"
	"htruong/kt-crawler/internal"
)

var (
	fetch = internal.Fetch
)

type Result struct {
	// Chapters []
}

func Run(ctx context.Context, book internal.Book) error {
	// loop chapters
	// scan chapter
	panic("TODO")
}
